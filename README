substruct site for musicformormons  (rails)

To use, create your vendor dir like this:
$ rm -rf vendor
$ svn checkout http://substruct.googlecode.com/svn/trunk/ vendor


Add in the admin_data plugin for rails2.3.x: https://github.com/neerajdotname/admin_data/wiki/Installing-admin_data-in-a-Rails-2.3.x-project
$ ruby script/plugin install http://github.com/neerajdotname/admin_data.git -r rails2


now create config/database.yml with mysql, then

$ rake gems:install
$ rake db:create
$ rake substruct:db:bootstrap
$ rake db:migrate

and it should be able to run.
